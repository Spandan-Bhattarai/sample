git init //Initialize git. creates .git flder

git remote add origin <github_repo_url> //links our remote repoository and local repository

git remote -v //to check our linked remote repo

git status // to check our changes

git add <file path> //to add our single file for comit
git add . //to add all of untracked files

git commit -m "short message" //git commit -m "short message -m "detailed message"

git branch //llocal branch
git branch /r // remote branch

git checkout -b <new_branch_name> 

git push origin master//to add to master branch
//
https://github.com/Spandan-Bhattarai

